#include "main.h"
/**
* _abs - absolute value
* @c: gets the value of the parameters
* Return: returns postive if negative or returns the same value
*/
int _abs(int c)
{
if (c > 0)
{
return (c);
}
else
{
return (-1 * c);
}
}
