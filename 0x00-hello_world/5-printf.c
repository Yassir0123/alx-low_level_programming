#include <stdio.h>
/**
* main - entery point of a function that will show the output of printf
* Return: always returns 0
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
